# Proposal

## Why
Create a CI/CD pipeline using github workflow.

## What
Create CI/CD pipeline using github workflow with lint, test, build and deploy actions.

## Scope
- On every push run lint and test stages
- On merge into main branch run lint, test, build and deploy actions.
- Use uv instead of pip
